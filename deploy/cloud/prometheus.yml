# Prometheus configuration for Expera AI
global:
  scrape_interval: 15s
  evaluation_interval: 15s

alerting:
  alertmanagers:
    - static_configs:
        - targets: []

rule_files: []

scrape_configs:
  - job_name: 'expera-api'
    static_configs:
      - targets: ['expera-api:8000']
    metrics_path: '/metrics'
    scrape_interval: 10s

  - job_name: 'prometheus'
    static_configs:
      - targets: ['localhost:9090']

  - job_name: 'nvidia-gpu'
    static_configs:
      - targets: ['expera-api:8000']
    metrics_path: '/metrics/gpu'